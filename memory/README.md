# Memory

This directory stores structured and unstructured memory for the agent.

Suggested split:
- `daily/` for raw event logs
- `long-term/` for distilled identity, preferences, and stable facts
- `scratch/` for temporary working notes
